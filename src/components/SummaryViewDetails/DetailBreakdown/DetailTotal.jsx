import React, { Component } from 'react';
import ReactDOM from 'react-dom';

class DetailTotal extends Component {
    render(){
        return(
            <div></div>
        )
    }
}

export default (DetailTotal)